# 运行与排障手册

## 1. 开发环境启动

### 1.1 基础命令
```bash
# 1) 启动基础设施
./scripts/dev-up.sh

# 2) 执行迁移（建议首次或显式检查时手动执行）
cd /home/wppjkw/smartphoto_backend
./.venv/bin/alembic upgrade head

# 3) 启动 API（会自动补一次 alembic upgrade head）
./scripts/dev-api.sh

# 4) 启动 Worker（会自动补一次 alembic upgrade head）
./scripts/dev-worker.sh
```

### 1.2 启动后检查
```bash
# 容器状态
docker compose ps

# 健康检查
curl -s http://127.0.0.1:8000/healthz
```

预期：`{"code":0,"message":"success","data":{"status":"ok"}}`

### 1.3 用户鉴权相关环境变量
```bash
# 用户 access token 签名密钥
USER_JWT_SECRET=change-me

# access token 过期分钟数
USER_ACCESS_TOKEN_EXP_MINUTES=120

# refresh token 过期天数
USER_REFRESH_TOKEN_EXP_DAYS=14

# 开发态是否允许未登录回落到固定 dev 用户
ALLOW_DEV_AUTH_BYPASS=true
```

说明：
- 本地调试前端默认可保留 `ALLOW_DEV_AUTH_BYPASS=true`
- 联调或验收用户功能时，建议显式改为 `false`，确保所有 `/api/v2` 私有接口都经过真实 Bearer 鉴权
- 若需要保留老数据可访问，迁移会自动补一条与 `TEST_USER_ID` 对齐的开发用户
- 调试前端当前会优先用 refresh cookie 恢复登录；若关闭 dev bypass，请先完成 `/api/v2/auth/login` 或 `/register`

### 1.3.2 跨域联调环境变量
```bash
# 允许跨域访问的前端 Origin，多个值可用逗号分隔或 JSON 数组
CORS_ALLOW_ORIGINS=http://localhost:5173
```

说明：
- 当前实现是显式 allowlist，不支持 `*`
- 前台 `/api/v2/*`、后台 `/api/admin/v1/*` 和 SSE `/jobs/{job_id}/events` 都受同一套 CORS 配置约束
- 若浏览器要携带 refresh cookie，请在前端请求侧打开 `credentials: "include"`

### 1.3.1 对象存储相关环境变量
```bash
STORAGE_BACKEND=local|s3
STORAGE_ROOT=./storage
S3_ENDPOINT=https://your-oss-endpoint
S3_REGION=auto
S3_BUCKET=smartphoto-private
S3_ACCESS_KEY=xxx
S3_SECRET_KEY=xxx
S3_FORCE_PATH_STYLE=false
S3_SIGNED_URL_TTL_SECONDS=900
S3_PRESIGN_UPLOAD_TTL_SECONDS=900
S3_PREFIX_UPLOADS=uploads
S3_PREFIX_GENERATED=generated
```

说明：
- 本地保留 `STORAGE_BACKEND=local`。
- 上线切到 `s3` 后，浏览器上传建议统一走 `/api/v2/uploads/presign -> 直传 -> /api/v2/uploads/complete`。
- DB 中保存的是稳定对象 key，不是永久 URL。

### 1.4 问题：`alembic upgrade head` 报 `No 'script_location' key found in configuration`
含义：当前目录不在项目根目录，Alembic 没有找到 [alembic.ini](/home/wppjkw/smartphoto_backend/alembic.ini)。

修复方式：
```bash
cd /home/wppjkw/smartphoto_backend
./.venv/bin/alembic upgrade head
```

补充说明：
- 不要在 `adminfront/` 或 `frontend/` 子目录直接执行 Alembic
- `scripts/dev-api.sh` / `scripts/dev-worker.sh` 已固定在项目根目录执行迁移

### 1.5 问题：`ModuleNotFoundError: No module named 'psycopg'`
含义：当前使用的 Python / Alembic 来自其他项目虚拟环境，或者本地环境只装了 `psycopg2` 没装 `psycopg`。

修复方式：
```bash
cd /home/wppjkw/smartphoto_backend
./.venv/bin/alembic upgrade head
```

排查路径：
1. 执行 `which alembic`，确认是否指向本项目 `.venv/bin/alembic`
2. 若不是，优先显式改用 `./.venv/bin/alembic`
3. 当前 Alembic 会优先补入本项目 `.venv` 的 site-packages，并自动在 `postgresql+psycopg://` / `postgresql+psycopg2://` 之间做兼容归一化
4. 若仍失败，再执行 `./.venv/bin/pip install -e .[dev]`

### 1.6 对象存储上线前自检
```bash
# 1) presign
curl -s -X POST http://127.0.0.1:8000/api/v2/uploads/presign \
  -H 'Authorization: Bearer <access_token>' \
  -H 'Content-Type: application/json' \
  -d '{"session_id":"<session_id>","upload_kind":"session_image","original_name":"front.jpg","content_type":"image/jpeg","size_bytes":12345,"display_order":1,"slot_type":"front"}'

# 2) complete
curl -s -X POST http://127.0.0.1:8000/api/v2/uploads/complete \
  -H 'Authorization: Bearer <access_token>' \
  -H 'Content-Type: application/json' \
  -d '{"upload_id":"<upload_id>"}'
```

建议至少验证：
1. presign 返回 `upload_url`
2. 直传后 complete 成功
3. `GET /sessions/{id}/images` 返回的 `url` 可访问
4. `GET /sessions/{id}/download` ZIP 正常

## 1.7 生产部署（单机 Docker Compose）

### 1.7.1 首次部署目录建议
```bash
mkdir -p /opt/smartphoto_backend
cd /opt/smartphoto_backend
git clone <your-repo> .
cp .env.prod.example .env.prod
mkdir -p runtime/postgres runtime/redis runtime/storage
```

说明：
- 推荐固定在 `/opt/smartphoto_backend`
- `runtime/storage` 用于持久化 `admin.sqlite3` 与本地兜底文件

### 1.7.2 首次启动命令
```bash
docker compose --env-file .env.prod -f docker-compose.prod.yml build api
docker compose --env-file .env.prod -f docker-compose.prod.yml up -d postgres redis
docker compose --env-file .env.prod -f docker-compose.prod.yml run --rm migrate
docker compose --env-file .env.prod -f docker-compose.prod.yml up -d api worker
```

验收：
```bash
curl -s http://127.0.0.1:8000/healthz
docker compose --env-file .env.prod -f docker-compose.prod.yml ps
docker compose --env-file .env.prod -f docker-compose.prod.yml logs api --tail=100
docker compose --env-file .env.prod -f docker-compose.prod.yml logs worker --tail=100
```

### 1.7.3 按 Git Tag 发布
```bash
bash scripts/deploy-prod.sh v2026.03.19-01
```

发布脚本行为：
1. `git fetch --tags`
2. `git checkout <tag>`
3. 构建 `api/worker/migrate` 镜像
4. 启动 `postgres/redis`
5. 执行 `alembic upgrade head`
6. 启动 `api/worker`

### 1.7.4 回滚
```bash
bash scripts/rollback-prod.sh v2026.03.18-02
```

说明：
- 只允许回滚到已知可用 tag
- 若本次迁移不是向后兼容迁移，不要直接回滚应用版本
- 当前回滚脚本不会执行迁移，只负责切回旧 tag 并重启 `api/worker`

## 2. 任务链路排障

### 2.1 问题：任务长期停在 queued
排查路径：
1. Worker 是否运行
2. Worker 是否监听正确队列（`q.analysis,q.copy,q.generation.main,q.generation.detail`）
3. Redis 是否可连通
4. Job 是否已创建但未分发

建议检查：
```bash
# 观察 worker 日志（如果通过终端直接启动，直接看终端输出）
# 如通过 docker/pm2 部署，请查看对应日志
```

SQL 辅助：
```sql
SELECT id, job_type, status, queued_at, started_at
FROM jobs
ORDER BY created_at DESC
LIMIT 20;
```

### 2.2 问题：任务停在 running
排查路径：
1. 看 `/jobs/{id}` 中 `stage/error_code/error_message`
2. 看 `/jobs/{id}/events` 是否仍有增量事件
3. 检查上游接口可用性（50201/50202）
4. 检查存储路径可写权限（`STORAGE_ROOT`）
5. 如需核对 prompt 和参考图引用，单独调用 `/sessions/{id}/prompts/preview`
6. 若本轮走详情页独立生成，改查 `/sessions/{id}/detail-pages/prompts/preview`

建议检查：
```bash
curl -s http://127.0.0.1:8000/api/v2/jobs/<job_id>
curl -N http://127.0.0.1:8000/api/v2/jobs/<job_id>/events
curl -s -X POST http://127.0.0.1:8000/api/v2/sessions/<session_id>/prompts/preview \
  -H 'Content-Type: application/json' \
  -d '{"instruction":"主体更靠中间","include_latest_assets":true}'
curl -s -X POST http://127.0.0.1:8000/api/v2/sessions/<session_id>/detail-pages/prompts/preview \
  -H 'Content-Type: application/json' \
  -d '{"instruction":"标题更清晰","include_latest_assets":true}'
```

Step 5 策略排查：
```bash
curl -s -X POST http://127.0.0.1:8000/api/v2/sessions/<session_id>/strategy/preview \
  -H 'Content-Type: application/json' \
  -d '{"planner_instruction":"白底图更标准，主图更像参考图","slot_preferences":[{"slot_id":"proof_authority","expression_mode":"certificate_proof","locked":true}]}'
```

### 2.3 问题：409 并发冲突（job_already_running）
含义：同 session 或同用户已有运行中的生图任务。

排查路径：
1. 查询该 session 是否存在 `queued/running` 的生成类任务
2. 如果 Redis 可用，检查锁键是否残留

SQL 辅助：
```sql
SELECT id, session_id, user_id, job_type, status
FROM jobs
WHERE job_type IN ('generate_gallery','regenerate_gallery','global_edit','regenerate_asset','generate_detail_page')
  AND status IN ('queued','running')
ORDER BY created_at DESC;
```

Redis 辅助：
```bash
redis-cli KEYS 'lock:*:generation'
```

### 2.4 问题：上游失败（50201 / 50202）
排查路径：
1. `.env` 中 `WHATAI_API_KEY` 是否配置
2. `WHATAI_API_BASE`、模型名是否正确（默认推荐 `https://api.whatai.cc`，代码会自动补 `/v1`）
3. 网络出口是否可访问上游
4. 上游返回结构是否变更

补充说明：
- 若上游返回 `400 Bad Request`，优先检查当前模型对应的请求协议是否正确
- `gemini-*` 文本模型当前通过 Gemini 官方 `generateContent` 协议请求
- 参数提取模型默认 `WHATAI_PARAMETER_MODEL=gemini-3.1-flash-lite-preview`
- Step 2 分析默认优先读取 `WHATAI_ANALYSIS_MODEL`；Step 5 planner 默认优先读取 `WHATAI_PLANNER_MODEL`
- 若当前把 `WHATAI_CHAT_MODEL` 配成了 `gemini-*-thinking-*` 这类重型思考模型，建议不要直接复用到 Step 2/Step 5；否则带图请求时延会明显升高
- Step 2 分析与 Step 5 planner 当前都会把 session 图片以内联图像内容发给上游，不再依赖本地 URL 可被公网访问
- 主图组默认优先走 `/v1/images/edits`，把参考图 multipart 上传给上游；若上游返回 `task_id`，再轮询任务结果
- 生图链路当前改为“批量提交全部任务 -> 集中轮询全部 task_id -> 并发下载结果”
- 默认轮询策略由 `IMAGE_POLL_PROFILE` 控制，缺省为 `5s x 6 + 10s x 12 + 15s x 20`，总窗口约 7.5 分钟；`NOT_START` 不视为失败
- 主图和详情页默认分发到独立队列：`q.generation.main` / `q.generation.detail`
- 日志会附带上游响应正文，便于直接定位模型参数或协议不兼容
- 若 Step 2 分析或 Step 5 planner 在对象存储环境下频繁出现 `The read operation timed out`，优先上调 `WHATAI_REQUEST_TIMEOUT_SECONDS`；当前默认 `180` 秒，避免带图分析请求在上游高峰期被客户端 `90s` 读超时截断
- 若 WhatAI 报 `aspect_ratio must be one of ...`，优先检查当前是否把像素尺寸（如 `1792x768`）误传给了 `/images/edits`；当前实现要求 edit 请求传 `aspect_ratio`，而不是 `size`

### 2.5 问题：登录 / 刷新 / 账户页请求返回 401 或 429
排查路径：
1. 是否忘记带 `Authorization: Bearer <access_token>`
2. 当前环境是否已关闭 `ALLOW_DEV_AUTH_BYPASS`
3. `user_refresh_token` Cookie 是否仍有效
4. `USER_JWT_SECRET` 是否在 API 重启后变更，导致旧 token 失效
5. Redis 是否可连通；登录/注册/刷新限流依赖 Redis 计数

建议检查：
```bash
curl -s -X POST http://127.0.0.1:8000/api/v2/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"user@example.com","password":"secret123"}'

curl -s http://127.0.0.1:8000/api/v2/account/overview \
  -H 'Authorization: Bearer <access_token>'
```

补充说明：
- `40101 unauthorized`：常见于 access token 失效、Cookie 丢失、当前用户被禁用
- `42901 rate_limited`：短时间内重复注册/登录/刷新过多，等限流窗口过后再试

### 2.6 问题：浏览器报 CORS 或 refresh cookie 不生效
排查路径：
1. `.env.prod` 中 `CORS_ALLOW_ORIGINS` 是否包含前端真实 Origin（协议 + 主机 + 端口）
2. 前端请求是否启用了 `credentials: "include"`
3. 是否误把 `CORS_ALLOW_ORIGINS=*` 当作可用配置；当前实现不支持
4. 先用浏览器 Network 面板检查响应头里是否有 `access-control-allow-origin`

建议检查：
```bash
curl -i -X OPTIONS http://127.0.0.1:8000/api/v2/auth/login \
  -H 'Origin: http://your-frontend-host:5173' \
  -H 'Access-Control-Request-Method: POST' \
  -H 'Access-Control-Request-Headers: content-type,authorization'
```

预期：
- 已允许的 Origin 会返回 `access-control-allow-origin: <origin>`
- 未允许的 Origin 不会返回该响应头，预检可能直接失败
- 若是本地联调误触发限流，可清理测试 Redis DB 后重试
- 若上游报 `Server disconnected without sending a response.`，优先检查该次 WhatAI 任务是否已在中转后台生成成功；当前实现会尽量通过任务结果轮询与图片下载重试拿回链接，避免重复提交生图请求
- `/images/edits` 若在提交阶段发生传输层断连，当前实现会先做请求级重试；若仍失败，只对当前单张图做内部重试，不会整组重跑
- 若要排查“为什么这张图是这样生成的”，优先看 `POST /sessions/{id}/prompts/preview` 的 `prompts`、`reference_manifest` 和 `latest_assets.generation_snapshot`
- 若当前走详情页链路，优先看 `POST /sessions/{id}/detail-pages/prompts/preview` 的 `prompts`、`product_reference_manifest`、`style_reference_manifest` 和 `latest_assets.generation_snapshot`
- 若白底图失败，优先检查 `latest_assets.generation_snapshot.white_bg_validation` 或 worker 日志中的白底校验指标；当前是否触发白底校验取决于 `requires_white_bg_validation` 能力标记，而不是固定 role 名称

### 2.5.1 问题：生成接口返回 `40201 insufficient_credits`
含义：当前用户钱包余额不足，后端拒绝创建生成任务。

排查路径：
1. 调 `GET /api/v2/account/pricing` 看当前动作价格
2. 调 `GET /api/v2/account/wallet` 看余额
3. 调 `GET /api/v2/account/wallet/transactions` 看最近扣费/退款流水
4. 需要补额度时，用后台接口 `POST /api/admin/v1/users/{user_id}/wallet/adjust`

补充说明：
- 扣费时机：任务被接受并创建 job 前
- 退款时机：任务最终 `failed` 且未产出任何 ready asset
- `dev` 环境下固定开发用户会自动补一笔调试额度

说明：当前实现在无 key 时会使用占位输出，便于本地链路调试。

### 2.4.1 问题：`UndefinedColumn: column assets.generation_snapshot does not exist`
含义：代码已经升级到依赖 `assets.generation_snapshot` 的版本，但数据库仍停在旧迁移。

排查路径：
1. 执行 `./.venv/bin/alembic current`
2. 确认当前 revision 是否落后于 `./.venv/bin/alembic heads`
3. 执行 `./.venv/bin/alembic upgrade head`
4. 再次访问 `GET /sessions/{id}/results`

补充说明：
- 当前 `./scripts/dev-api.sh` 与 `./scripts/dev-worker.sh` 启动前会自动补一次迁移
- 若 API/Worker 已经在旧 schema 上运行，补完迁移后通常无需重建数据库，只需继续使用或重启进程

### 2.4.2 问题：详情页接口报缺列或缺表（如 `sessions.detail_strategy_preview` / `detail_style_images` / `assets.asset_family`）
含义：代码已经升级到包含详情页独立生成版本，但数据库仍停在旧迁移。

排查路径：
1. 执行 `./.venv/bin/alembic current`
2. 确认当前 revision 是否落后于 `./.venv/bin/alembic heads`
3. 执行 `./.venv/bin/alembic upgrade head`
4. 再次访问 `POST /sessions/{id}/detail-pages/strategy/preview`

### 2.5 问题：下载 ZIP 为空或版本不符
排查路径：
1. 先查 `GET /sessions/{id}/results` 看 `latest_result_version`
2. 下载接口是否指定了错误 `version`
3. 对应版本 `assets` 是否为 `ready`
4. 存储文件是否存在于 `STORAGE_ROOT`

SQL 辅助：
```sql
SELECT id, version_no, status, image_url
FROM assets
WHERE session_id = '<session_id>'
ORDER BY version_no DESC, display_order ASC;
```

### 2.6 问题：单图重生成后旧版本看不到
排查路径：
1. 先查 `GET /sessions/{id}/results` 的 `available_versions`
2. 再查 `GET /sessions/{id}/results?version=<旧版本>` 是否还有旧版本快照
3. 若是旧数据在历史实现中被错误标成 `superseded`，执行修复脚本

修复命令：
```bash
./.venv/bin/python scripts/repair_version_history.py
```

补充说明：
- 当前实现已改成“历史版本保留 + 单图重生成物化完整新版本”
- 修复脚本会恢复旧版本可见性，并补齐缺失的 carry-forward 资产

## 3. 最小观测项（建议）
- Job 总量（按类型）
- Job 成功率/失败率（按类型）
- 平均耗时（analysis/copy/generation）
- 失败码分布（400xx/409xx/502xx）
- 单 session 的重生成次数

## 4. 联调脚本化检查清单
以下流程用于验证最短可用链路：
1. 创建会话
2. 上传图片
3. 触发分析
4. 选择平台
5. 保存 copy
6. 生成策略预览
7. 触发生图
8. 查询 job 与 SSE
9. 查询结果
10. 查看 prompt preview（可选）
11. 下载 ZIP

示例（按需替换变量）：
```bash
BASE=http://127.0.0.1:8000/api/v2
SID=$(curl -s -X POST "$BASE/sessions" | jq -r '.data.session_id')

curl -s -X POST "$BASE/sessions/$SID/images" \
  -F "file=@./demo.jpg;type=image/jpeg" \
  -F "slot_type=front" \
  -F "display_order=1"

curl -s -X POST "$BASE/sessions/$SID/analysis"
curl -s -X PUT "$BASE/sessions/$SID/platform-selection" \
  -H 'Content-Type: application/json' \
  -d '{"selected_platform_ids":["temu"],"active_platform_id":"temu"}'

curl -s -X PUT "$BASE/sessions/$SID/copy" \
  -H 'Content-Type: application/json' \
  -d '{"product_name":"测试产品","category":"测试类目","hero_scene":"客厅净化","core_selling_points":["卖点A"],"key_parameters":[{"label":"参数A","value":"100","unit":""}],"product_advantages":["优势A"],"style_preset_id":null,"style_custom":"现代简约"}'

curl -s -X POST "$BASE/sessions/$SID/strategy/preview"
curl -s -X POST "$BASE/sessions/$SID/strategy/preview" \
  -H 'Content-Type: application/json' \
  -d '{"planner_instruction":"白底图更标准，主图更像参考图","slot_preferences":[{"slot_id":"proof_authority","expression_mode":"certificate_proof","locked":true}]}'
JOB_ID=$(curl -s -X POST "$BASE/sessions/$SID/generations" \
  -H 'Content-Type: application/json' -d '{"instruction":null}' | jq -r '.data.job_id')

# 只调试单个主图槽位
SINGLE_JOB_ID=$(curl -s -X POST "$BASE/sessions/$SID/generations" \
  -H 'Content-Type: application/json' \
  -d '{"instruction":"先试一张","slot_ids":["proof_authority"]}' | jq -r '.data.job_id')

curl -s "$BASE/jobs/$JOB_ID"
curl -s "$BASE/sessions/$SID/results"
curl -s -X POST "$BASE/sessions/$SID/prompts/preview" \
  -H 'Content-Type: application/json' \
  -d '{"instruction":"主体更靠中间","include_latest_assets":true}'

# 详情页独立生成链路（可替代主图生成）
curl -s -X POST "$BASE/sessions/$SID/detail-pages/style-images" \
  -F "file=@./style.jpg;type=image/jpeg" \
  -F "display_order=1"
curl -s -X POST "$BASE/sessions/$SID/detail-pages/strategy/preview" \
  -H 'Content-Type: application/json' \
  -d '{"planner_instruction":"标题更短，版式更清晰","panel_preferences":[{"slot_id":"detail_slot_02","panel_type":"parameter_explainer","display_order":2,"locked":true}]}'
DETAIL_JOB_ID=$(curl -s -X POST "$BASE/sessions/$SID/detail-pages/generations" \
  -H 'Content-Type: application/json' -d '{"instruction":"整体更高级"}' | jq -r '.data.job_id')
curl -s "$BASE/jobs/$DETAIL_JOB_ID"
curl -s "$BASE/sessions/$SID/detail-pages/results"
```

## 5. 文档维护要求
- 每次新增/修改接口，必须同步更新：
  - `docs/API_联调指南.md`
  - 如涉及执行语义，更新 `docs/生图Agent协作逻辑.md`
  - 如涉及排障路径，更新本文档

## 6. 后台管理快速启动
1. 初始化管理员账号：
```bash
./.venv/bin/python scripts/create_admin_user.py --username admin --password secret123 --display-name 管理员
```
2. 后台接口前缀：`/api/admin/v1`
3. 启动后台前端：
```bash
cd adminfront
npm install
npm run dev
```
4. 后台前端默认端口：`3001`
